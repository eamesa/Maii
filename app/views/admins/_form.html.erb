<%= form_with(model: admin, local: true) do |form| %>
  <% if admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin.errors.count, "error") %> prohibited this admin from being saved:</h2>

      <ul>
        <% admin.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre_admin %>
    <%= form.text_field :nombre_admin %>
  </div>

  <div class="field">
    <%= form.label :tipo_admin %>
    <%= form.text_field :tipo_admin %>
  </div>

  <div class="field">
    <%= form.label :codigo_admin %>
    <%= form.text_field :codigo_admin %>
  </div>

  <div class="field">
    <%= form.label :fondos_count %>
    <%= form.number_field :fondos_count %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
