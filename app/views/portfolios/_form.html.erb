<%= form_with(model: portfolio, local: true) do |form| %>
  <% if portfolio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(portfolio.errors.count, "error") %> prohibited this portfolio from being saved:</h2>

      <ul>
        <% portfolio.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :nombre_portafolio %>
    <%= form.text_field :nombre_portafolio %>
  </div>

  <div class="field">
    <%= form.label :investments_count %>
    <%= form.number_field :investments_count %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
